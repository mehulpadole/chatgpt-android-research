# Realtime Voice

`RealtimeVoiceAdapter` is defined as an independent contract with connected, transcript, audio, closed, and error callbacks. No provider-specific realtime transport is claimed in this phase; live realtime voice is Not tested and remains an explicit future adapter implementation.
