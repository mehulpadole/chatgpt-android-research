# Prototype verification fixtures

The performance harness creates deterministic in-memory datasets of 0, 10,
100, and 1,000 messages, a 10,000-chunk stream, and 100 attachment metadata
records. It does not contain user data or provider credentials. Run it through
`run-core-tests.sh`; the benchmark lines are copied into the Phase 12 report.
