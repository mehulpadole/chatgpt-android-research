package com.example.androidfeasibility;

public enum MockScenario {
    NORMAL,
    SLOW,
    FAIL_BEFORE_CONTENT,
    FAIL_AFTER_PARTIAL,
    EMPTY,
    DUPLICATE_TERMINAL,
    DELTA_AFTER_TERMINAL,
    FAIL_AFTER_TERMINAL
}
