package com.example.androidfeasibility;

public final class PrivacyDisclosure {
    public static final String TEXT =
            "MoCHi Android stores conversations and attachment metadata locally. "
            + "Provider credentials stay in the platform credential boundary and are not exported or synced. "
            + "Optional cloud sync is staging-only in this prototype; a future server may read synced records. "
            + "This prototype does not claim end-to-end encryption or production account privacy guarantees.";

    private PrivacyDisclosure() { }
}
